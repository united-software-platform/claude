# Контекст сборки образа Claude Code: Dockerfile ничего не копирует,
# поэтому в контекст не передаётся ничего лишнего — сборка быстрее,
# а секреты окружения заведомо не попадают в демон Docker.
.git/
.github/
.idea/
.venv/
.env
.ssh/
.gh/
.claude-accounts/
data/
*.log
